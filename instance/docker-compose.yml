# ###################################
# This file is generated by puppet
# PLEASE DON'T MODIFY BY HAND
# ###################################
version: '2'
services:

########################
# CHE
########################
  che:
    image: eclipse/che-server:nightly
    env_file:
      - 'c:\codenvy\che\instance\config\che\che.env'
    volumes:
       - '/var/run/docker.sock:/var/run/docker.sock'
       - '/c/codenvy/che/instance/data:/data'
       - '/c/codenvy/che/instance/config/che/conf:/c/codenvy/che/instance/config/che/conf'
    ports:
      - '8080:8080'
    restart: always
    container_name: che
